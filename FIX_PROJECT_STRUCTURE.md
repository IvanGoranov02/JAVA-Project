# Как да поправиш Project Structure - Избери правилния модул!

## ⚠️ Проблем
В Project Structure диалога виждаш **`maven-wrapper`** модула избран, но трябва да избереш **`shoe-warehouse`** модула!

---

## ✅ Правилна стъпка по стъпка настройка:

### Стъпка 1: Избери правилния модул

В Project Structure диалога:

1. В **средния панел** (списък с модули), виждаш два модула:
   - `maven-wrapper` ← НЕ ТОЗИ!
   - `shoe-warehouse` ← ИЗБЕРИ ТОЗИ!

2. **Кликни на `shoe-warehouse`** (не на `maven-wrapper`!)

---

### Стъпка 2: Маркирай Source папките за `shoe-warehouse`

След като избереш `shoe-warehouse` модула:

1. В **десния панел**, под **Content Root**, трябва да виждаш:
   - Път: `C:\Users\FAMILY\JAVA-Project2` (или подобен)

2. Под Content Root, намери папките:
   - `main/java` 
   - `test/java`
   - `main/resources`
   - `test/resources`

3. **Маркирай папките:**
   - Избери папката `main/java`
   - Кликни на **синята икона** (Sources) отгоре, или
   - Кликни десен бутон → **Mark Directory as → Sources Root**
   
   - Избери папката `test/java`
   - Кликни на **зелената икона** (Tests) отгоре, или
   - Кликни десен бутон → **Mark Directory as → Test Sources Root**
   
   - Избери папката `main/resources`
   - Кликни на **кафявата икона** (Resources) отгоре
   
   - Избери папката `test/resources`
   - Кликни на **светло зелената икона** (Test Resources) отгоре

4. В **десния панел** (Content Root Summary), трябва да виждаш:
   - **Source Folders:** `main\java` (в зелено)
   - **Test Source Folders:** `test\java` (в зелено)
   - **Resource Folders:** `main\resources` (в зелено)
   - **Test Resource Folders:** `test\resources` (в зелено)

5. Кликни **Apply** и **OK**

---

### Стъпка 3: Build проекта

1. **Build → Build Project** (`Ctrl+F9`)
2. Изчакай да завърши
3. Провери дали има `target/classes` папка

---

### Стъпка 4: Инвалидирай кеша

1. **File → Invalidate Caches...**
2. Избери **Invalidate and Restart**
3. След рестарт, изчакай IntelliJ да реиндексира

---

### Стъпка 5: Стартирай приложението

След като направиш всичко:

1. Отвори `ShoeWarehouseApplication.java` в редактора
2. Кликни на **зелената стрелка (▶)** до `main()` метода
3. Или: **Run → Run 'ShoeWarehouseApplication'**

---

## 🔍 Проверка след настройката

След като направиш промените:

1. В Project view, `main/java` трябва да има **синя икона** (Sources)
2. В Project view, `test/java` трябва да има **зелена икона** (Test Sources)
3. При Build → Build Project, не трябва да има грешки
4. При Run, приложението трябва да стартира

---

## ❓ Ако все още не работи

Провери:
1. Дали си избрал **`shoe-warehouse`** модула (не `maven-wrapper`!)
2. Дали папките са маркирани правилно (синя, зелена икона)
3. Дали има `target/classes` папка след Build

---

## 💡 Запомни

**ВАЖНО:** В Project Structure, **ВИНАГИ избирай `shoe-warehouse` модула**, не `maven-wrapper`!

`maven-wrapper` е технически модул за Maven wrapper, а твоят код е в `shoe-warehouse` модула!
