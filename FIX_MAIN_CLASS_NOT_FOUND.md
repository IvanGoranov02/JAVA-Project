# Как да поправиш "Main class не се намира"

## Проблем
IntelliJ не може да намери `com.shoewarehouse.ShoeWarehouseApplication`

---

## ✅ Решение 1: Използвай файла директно (НАЙ-ЛЕСНО)

### Стъпка 1: Отвори файла
1. В Project view, намери: `main/java/com/shoewarehouse/ShoeWarehouseApplication.java`
2. Двойно кликни, за да го отвориш в редактора

### Стъпка 2: Стартирай от файла
1. В редактора, намери метода `public static void main(...)`
2. До реда с `main()` или до `@SpringBootApplication` трябва да има зелена стрелка (▶)
3. Кликни на зелената стрелка → **Run 'ShoeWarehouseApplication.main()'**

IntelliJ ще създаде конфигурацията автоматично!

---

## ✅ Решение 2: Поправи Source папките и компилирай

### Стъпка 1: Маркирай Source папките
1. **File → Project Structure** (`Ctrl+Alt+Shift+S`)
2. Отиди на **Modules**
3. Избери модула `shoe-warehouse`
4. Под **Sources**, провери папките:
   - `main/java` → трябва да е **Sources** (синя папка)
   - `test/java` → трябва да е **Test Sources** (зелена папка)
5. Ако не са маркирани:
   - Кликни десен бутон на папката → **Mark Directory as** → избери типа
6. Кликни **Apply** и **OK**

### Стъпка 2: Компилирай проекта
1. **Build → Build Project** (`Ctrl+F9`)
2. Изчакай да завърши
3. Провери дали има `target/classes` папка

### Стъпка 3: Индексирай проекта отново
1. **File → Invalidate Caches...**
2. Избери **Invalidate and Restart**
3. След рестарт, изчакай IntelliJ да реиндексира (ще видиш "Indexing..." отдолу)

---

## ✅ Решение 3: Използвай Maven за компилиране

### Стъпка 1: Компилирай с Maven
1. Отвори **Maven** tool window (View → Tool Windows → Maven)
2. Разгъни: `shoe-warehouse` → **Lifecycle**
3. Двойно кликни на **compile**
4. Изчакай да завърши

### Стъпка 2: След това използвай Application конфигурация
1. Сега IntelliJ трябва да намери класа
2. Създай Application конфигурация
3. Main class: `com.shoewarehouse.ShoeWarehouseApplication`

---

## ✅ Решение 4: Напиши main class ръчно с пътя

Ако IntelliJ все още не го намира:

1. В Application конфигурацията, вместо да избираш от списъка
2. Напиши директно: `com.shoewarehouse.ShoeWarehouseApplication`
3. Кликни **OK**
4. Опитай да стартираш - ако има грешка, поне ще видим каква е

---

## ✅ Решение 5: Провери дали файлът е в правилната папка

Уверете се че файлът е тук:
```
C:\Users\FAMILY\JAVA-Project2\main\java\com\shoewarehouse\ShoeWarehouseApplication.java
```

И съдържа:
```java
package com.shoewarehouse;

@SpringBootApplication
public class ShoeWarehouseApplication {
    public static void main(String[] args) {
        SpringApplication.run(ShoeWarehouseApplication.class, args);
    }
}
```

---

## 💡 Препоръка

**Използвай Решение 1** - просто отвори файла и кликни на зелената стрелка до `main()` метода. Това е най-лесното!

---

## След стартиране

След като стартираш, ще видиш:
```
Started ShoeWarehouseApplication in X.XXX seconds
```

И можеш да тестваш в Postman на `http://localhost:8080/api/brands`
