# Как да поправиш "Не ми дава да избера класа" в IntelliJ

## Проблем
IntelliJ показва файла `ShoeWarehouseApplication.java` но не го разпознава като валиден main class.

---

## ✅ Решение 1: Маркирай Source папките ПЪРВО (ВАЖНО!)

### Стъпка 1: Отвори Project Structure
1. **File → Project Structure** (или `Ctrl+Alt+Shift+S`)
2. Отиди на **Modules**

### Стъпка 2: Маркирай Source папките
1. Избери модула `shoe-warehouse` (или името на проекта)
2. Под **Sources**, виж папките:
   - `main/java` → трябва да е **маркирана като Sources** (синя папка)
   - `test/java` → трябва да е **маркирана като Test Sources** (зелена папка)

3. **Ако не са маркирани:**
   - Кликни **десен бутон** на `main/java` папката
   - Избери **Mark Directory as → Sources Root**
   - Кликни **десен бутон** на `test/java` папката
   - Избери **Mark Directory as → Test Sources Root**

4. Кликни **Apply** и **OK**

### Стъпка 3: Реиндексирай проекта
1. **File → Invalidate Caches...**
2. Избери **Invalidate and Restart**
3. След рестарт, изчакай IntelliJ да реиндексира (ще видиш "Indexing..." отдолу)

---

## ✅ Решение 2: Компилирай проекта

### Стъпка 1: Build проекта
1. **Build → Build Project** (`Ctrl+F9`)
2. Изчакай да завърши
3. Провери дали има `target/classes` папка

### Стъпка 2: След това опитай отново
1. Отвори "Edit Configuration" отново
2. Опитай да избереш класа - трябва да работи сега

---

## ✅ Решение 3: Въведи Main class ръчно

Ако все още не работи:

1. В "Edit Configuration" диалога
2. В полето **Main class**, вместо да избираш от списъка
3. **Напиши директно**: `com.shoewarehouse.ShoeWarehouseApplication`
4. Кликни **OK**

IntelliJ ще приеме това, дори да не го разпознава автоматично.

---

## ✅ Решение 4: Провери дали файлът е правилен

Провери дали `ShoeWarehouseApplication.java` съдържа:

```java
package com.shoewarehouse;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ShoeWarehouseApplication {

    public static void main(String[] args) {
        SpringApplication.run(ShoeWarehouseApplication.class, args);
    }
}
```

Ако липсва `main` метод, добави го!

---

## 🔍 Бърза проверка

След маркиране на Source папките:
1. Файлът `ShoeWarehouseApplication.java` трябва да няма червени линии под `shoewarehouse`
2. В Project view, `main/java` трябва да има синя икона (Sources)
3. При Build → Build Project, не трябва да има грешки

---

## 💡 Препоръка

**Направи Решение 1 първо** - това е най-често срещаният проблем. След като маркираш source папките и реиндексираш, IntelliJ трябва да разпознае класа.

След това опитай **Решение 3** - просто напиши `com.shoewarehouse.ShoeWarehouseApplication` ръчно в Main class полето.
