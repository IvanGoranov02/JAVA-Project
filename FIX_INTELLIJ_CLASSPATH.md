# Как да поправиш "Could not find or load main class" - Липсва компилирани класове

## Проблем
IntelliJ не може да намери компилираните класове. Classpath-ът включва само Maven JAR файлове, но не и твоите компилирани класове.

---

## ✅ Решение: Компилирай проекта ПЪРВО

### Стъпка 1: Компилирай с Maven в IntelliJ

1. **Отвори Maven tool window:**
   - View → Tool Windows → Maven
   - Или: кликни десен бутон на `pom.xml` → Maven → Show Maven Tool Window

2. **Компилирай проекта:**
   - Разгъни: `shoe-warehouse` → **Lifecycle**
   - Двойно кликни на **compile** (или **clean compile**)
   - Изчакай да завърши (ще видиш логове отдолу)

3. **Проверка:**
   - Трябва да видиш папка `target` в проекта
   - Трябва да има `target/classes/com/shoewarehouse/ShoeWarehouseApplication.class`

---

### Стъпка 2: Или Build от менюто

1. **Build → Build Project** (или `Ctrl+F9`)
2. Изчакай да завърши
3. Провери дали има `target/classes` папка

---

### Стъпка 3: Провери Run Configuration

1. **Кликни на dropdown до Run бутона** → **Edit Configurations...**
2. Избери конфигурацията `ShoeWarehouseApplication`
3. Провери **Use classpath of module:** трябва да е `shoe-warehouse`
4. **Working directory:** трябва да е папката на проекта (`C:\Users\FAMILY\JAVA-Project`)
5. Кликни **Apply** и **OK**

---

### Стъпка 4: Рестартирай приложението

1. Затвори текущия Run (ако е стартиран)
2. Кликни **Run** отново (зелената стрелка)

---

## 🔍 Бърза проверка

След компилиране, провери:

1. Има ли папка `target/classes`?
2. Има ли `target/classes/com/shoewarehouse/ShoeWarehouseApplication.class`?
3. Ако няма → компилирай отново (Метод 1 или 2)

---

## ❓ Ако все още не работи

1. **Build → Rebuild Project**
2. **File → Invalidate Caches...** → **Invalidate and Restart**
3. След рестарт, **Build → Build Project**
4. Опитай отново да стартираш

---

## 💡 Забележка

IntelliJ трябва автоматично да включва `target/classes` в classpath. Ако не го прави:
- Провери дали `target/classes` е маркиран като **Output** папка
- File → Project Structure → Modules → `shoe-warehouse` → Paths → Compiler output
